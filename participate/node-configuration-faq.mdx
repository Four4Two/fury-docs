---
sidebar_position: 2
title: Node Configuration FAQs
---

# FAQs

## General Node Setup

Q. Where to find/download genesis file?

A. The latest genesis file for the Fury mainnet is located [here]( https://fury-genesis-files.s3.us-east-1.amazonaws.com/highbury_710-1/genesis.json).

Q. Where can I get a list of seeds node addresses to populate the [p2p] section of config.yml `seeds` value?

A. [Here](https://polkachu.com/seeds/fury)

Q.  Where can I get a list of seeds node addresses to populate the [p2p] of config.yml `persistent_peers` value?

A. [Here](https://polkachu.com/live_peers/fury)

Q. What should be the value of `minimum-gas-prices` in app.toml

A. `0.001ufury;1000000000afury`

Q. Where can I get a complete example of standard settings for `app.toml`, `config.toml`, and `client.toml` node configuration files?

A. [app.toml](../../static/files/fury.service/app.toml), [config.toml](../../static/files/fury.service/config.toml), [client.toml](../../static/files/fury.service/client.toml)


Q. Are the EVM APIs enabled by default?

A. Yes

Q. What ports are used by the EVM API servers?

A. By default, the RPC EVM API server listens on port `8545`, and the Websocket EVM API server listens on port `8546`. These values can be changed by updating the `address` and `ws-address` field values in the `[evm]` section of your node's app.toml and restarting the fury process.
